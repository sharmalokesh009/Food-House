import React from "react";

export default function LoadingIcon(props){
    return <div  className="loadingicon-container">
        <div style={{width : props.width , height:props.height , marginLeft : props.marginleft , marginTop : props.margintop}}>
            
        </div>
    </div>
}